# .NET Version Upgrade Progress

## Overview

Upgrade the single SDK-style project from .NET Core 3.1 to .NET 10.0 (LTS) and adjust the small dependency set to match the new target framework. The solution is compact, so the work is planned as a single focused task followed by build validation.

**Progress**: 1/1 tasks complete (100%) ![100%](https://progress-bar.xyz/100)

## Tasks

- ✅ 01-upgrade-bubenbot-project: Upgrade `BubenBot.csproj` to .NET 10.0 and validate the build
